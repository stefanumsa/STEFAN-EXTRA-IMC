html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title STEFAN-ORDINARIO-CLIENTE-
    link(rel="stylesheet", href="/css/style.css")
  body
    nav
      ul
        li
          a(href='/') Inicio
        li.dropdown
          a(href='#') -Agregar-
          .dropdown-content
            a(href='/cesar') Agregar Cliente.
            a(href='/sustitucion') Agregar Oportunidad de Venta.
            a(href='/hexadecimal') Agregar Informe. 

        li.dropdown
          a(href='#') -Ver-
          .dropdown-content
            a(href='/clientes') Ver Clientes.
            a(href='/ventas') Ver Oportunidad de Venta.
            a(href='/informes') Ver Informes.      

    .container
    block content
    h1 Registro de Interacciones con Clientes
    <div class="container">
       a(href='/interacciones') Ver Registro de Interacciones.</a>
    </div>
      .registro-container
        form(action="/add-interaccion" method="POST")
          

         .form-group
           label(for="tipo") Tipo de Interacción:
           select(id="tipo" name="tipo" required)
             option(value="Llamada") Llamada
             option(value="Correo Electrónico") Correo Electrónico
             option(value="Reunión") Reunión
             option(value="Otro") Otro
          
           label(for="fecha") Fecha de la Interaccion:
           input(type="date" id="fecha" name="fecha" required)
          
           label(for="descripcion") Descripción de la Interaccion:
           textarea(id="descripcion" placeholder='Descripción de la Interaccion' name="descripcion" required)


          
           button(type="submit") Agregar Interacción
      